[tool.poetry]
name = "docker-prioritizer"
version = "0.1.0"
description = ""
authors = ["Your Name <you@example.com>"]
readme = "README.md"

[tool.poetry.dependencies]
# Downgrading to be compatible with older, stable Linux environments
python = "^3.8"
typer = {extras = ["all"], version = "^0.12.5"}
pydantic = "^1.10.0"  # Re-adding the missing pydantic dependency
pandas = "~1.5.3"
numpy = "~1.24.3"
pyyaml = "^6.0.3"
rich = "^13.9.4"

[tool.poetry.group.dev.dependencies]
pytest = "^7.4.4"
black = "^23.12.1"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
