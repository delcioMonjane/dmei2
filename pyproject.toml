[tool.poetry]
name = "docker-prioritizer"
version = "0.1.0"
description = ""
authors = ["Your Name <you@example.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.12"
typer = {extras = ["all"], version = "^0.12.5"}
pandas = "~1.5.3"  # Downgraded from ^2.0.0
numpy = "~1.24.3"  # Explicitly pinning numpy version compatible with pandas 1.5
pyyaml = "^6.0.3"
rich = "^13.9.4"

[tool.poetry.group.dev.dependencies]
pytest = "^7.4.4"
black = "^23.12.1"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
